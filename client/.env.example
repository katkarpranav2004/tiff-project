# Base URL of the API server (no trailing /api — the client appends it).
# Local dev:
VITE_API_URL=http://localhost:5000
# Production (set this in Vercel project env, not here):
# VITE_API_URL=https://your-api.onrender.com
